package com.nexora.savora.ui.theme

import androidx.compose.ui.graphics.Color

// Dark (default) — pure black base with white glass layers
val BlackBackground = Color(0xFF050505)
val BlackSurfaceElevated = Color(0xFF111111)

// Light — crisp white with black glass layers
val WhiteBackground = Color(0xFFF5F5F5)
val WhiteSurfaceElevated = Color(0xFFECECEC)

// Neutrals
val MonoWhite = Color(0xFFFFFFFF)
val MonoBlack = Color(0xFF000000)
val MonoGrayLight = Color(0xFFD0D0D0)
val MonoGray = Color(0xFF9E9E9E)
val MonoGrayDark = Color(0xFF555555)

// Graphite glass surfaces (solid, premium dark)
val MonoCardTop = Color(0xFF202020)
val MonoCardBottom = Color(0xFF111111)
val MonoPanel = Color(0xFF161616)
val MonoNavBar = Color(0xFF0F0F0F)
val MonoSnackBar = Color(0xFF1E1E1E)

// Glass helpers (used as alpha overlays)
val WhiteGlass = Color(0x14FFFFFF)
val WhiteGlassBorder = Color(0x33FFFFFF)
val BlackGlass = Color(0x14000000)
val BlackGlassBorder = Color(0x33000000)